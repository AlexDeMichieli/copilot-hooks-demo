# Example file in src/ directory
# Copilot can edit this file (it's inside src/)

def greet(name: str) -> str:
    """Simple greeting function."""
    return f"Hello, {name}!"

if __name__ == "__main__":
    print(greet("World"))
